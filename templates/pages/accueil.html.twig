{% extends "basefront.html.twig" %}

{% block body %}
    <main>
        <div id="content">
            <!--Alternance annonce-->
            <div id="alternanceAnnouncement" class="w-100">
                <h4>Je suis actellement à la recherche d'une alternance !</h4>
                <div class="h-100">
                    <p>Vous recherchez un apprenti ?</p>
                    <form action="contact.html">
                        <button class="w-100 h-100 border-0">Contactez moi !</button>
                    </form>
                </div>
            </div>
            <!--Image de decoration-->
            <div class="m-auto w-50">
                <img src="{{ asset("img/working.gif") }}">
            </div>
        </div>
    </main>
{% endblock %}